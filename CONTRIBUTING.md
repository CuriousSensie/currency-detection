# Contributing

Use a focused branch and keep `main` runnable. Before proposing a change, run `make verify`. Data and model work must include manifest/config/checkpoint hashes and retain failed or omitted experiment states. Never add raw currency images, API keys, model binaries, or manually typed performance values to Git.

Bug reports should include the request ID, model versions, operating thresholds, input properties without private image content, and reproduction steps.
